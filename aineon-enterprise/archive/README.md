# Archive

This directory contains deprecated files from previous development phases. These files are kept for historical purposes but should not be used. The active development has been consolidated into the `/api` and `/dashboard` directories.
